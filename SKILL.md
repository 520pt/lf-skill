---
name: lufei-lessons
description: 王路飞的个人工作规则与长期踩坑记忆。用于软件开发、Windows 工具、Docker/GitHub 部署、UI 还原、APK 逆向、OCR/图片、水印、通知系统和复杂问题排查；解决新的非明显问题后更新复盘记录。
---

# Lufei Lessons

这是王路飞的个人工作手册。目标不是积累大量技术术语，而是让 Codex 更准确地理解需求、减少返工，并把问题验证到真实可用。

## 四项总原则

- 编码前先思考：明确目标、假设、歧义、风险和可选方案，不把不确定的判断藏起来。
- 简洁优先：用能稳定解决问题的最少代码和最少依赖，不为未来可能出现的需求提前堆抽象。
- 精准修改：只修改任务需要的范围；只清理本次改动造成的无用内容，不顺手整理旧代码。
- 目标驱动验证：把“做一个功能”转成可检查的完成标准，按“修改一步、验证一步”循环，直到结果达标。

简单的一行修改、拼写修正或明确的只读检查可以轻量处理；复杂任务才需要完整计划、测试和多轮验证。

## 使用规则

开始项目工作前：

- 先读取 `references/personal-rules.md`。
- 涉及 Windows、中文、PowerShell、路径、构建或进程时，再读取 `references/windows-pitfalls.md`。
- 涉及跨项目工程判断、状态、缓存、验证、UI、部署或并行任务时，再读取 `references/engineering-patterns.md`。
- 涉及代码定位、影响范围、极简实现、精准修改或完整验证流程时，读取 `references/ai-coding-workflow.md`。
- 涉及 Android/TV、图片/OCR、水印、排班/通知或文档导出时，读取该任务相关章节。
- 同时检查项目根目录的 `AGENTS.md`、`MEMORY.md`、README、配置和启动文件；项目规则优先。

## 公开备份

- 本 skill 的通用规则和安装脚本同步到 `https://github.com/520pt/lf-skill`。
- 本机 `references/lesson-log.md` 默认是私有复盘记录，可能包含本地路径、项目细节或其他隐私信息，不直接同步到公开仓库。
- 修改可公开文件并通过验证后，使用仓库的同步脚本提交并推送；已配置 Git 提交钩子时，提交完成会自动推送。
- 自动推送失败时必须明确报告失败原因，不能把“本地提交成功”说成“GitHub 已同步”。

## 工作原则

- 先理解真实目标和完成标准，不要自行脑补关键需求。
- 能从项目、运行环境或已有实现中查到的信息，先主动检查，不要把可以发现的问题直接抛给用户。
- 只有缺少的信息无法通过检查获得，且错误假设会造成明显返工、数据损坏或外部影响时，才向用户提问。
- 同一任务中用户已经明确授权的连续操作，在目标、账号、数据范围和风险等级不变时直接继续，不重复询问；出现新的目标或风险时重新确认。
- 修复非明显、重复或可能复发的问题后，将根因、修复、验证和防复发规则写入 `references/lesson-log.md`。
- 不记录密码、令牌、私钥、Cookie 或其他敏感值；只记录配置位置和处理方式。

## 记录边界

- 跨项目通用规则放入长期参考文件。
- Windows 通用问题放入 `windows-pitfalls.md`。
- 具体项目的字段、端口、接口、默认值和产品决策放入项目自己的 `AGENTS.md` 或 `MEMORY.md`。
- `lesson-log.md` 只记录有复用价值的根因，不把每一次临时需求变化都当成长期规则。
- 同一根因再次出现时，优先补充或合并原记录，并标记已被替代的旧规则。

## 交付

完成前应根据风险运行适合的测试、检查、构建、真实流程或截图验证。无法做真实验证时，必须明确说明未验证范围，不能把中间结果报告为最终完成。

更新本 skill 后，运行：

```powershell
python -X utf8 C:/Users/Administrator/.codex/skills/.system/skill-creator/scripts/quick_validate.py C:/Users/Administrator/.codex/skills/lufei-lessons
```
